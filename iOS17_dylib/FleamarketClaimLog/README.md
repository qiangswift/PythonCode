# 闲鱼领取诊断 0.5.0

只注入 `com.taobao.fleamarket`；只读记录请求和屏幕出现的两种领取失败文案时间，不更改请求、响应或设备环境。日志位于闲鱼沙盒 `Documents/FleamarketClaimLog.log`。0.5.0 根据 0.4.0 日志中“点击后只有页面静态资源、没有 fetch/XHR 业务请求”的证据，为“我的 → 闲鱼币 → 赚骰子 → 领取奖励”增加 WindVane JS→原生桥接调用和回调的白名单错误字段探针。它只记录桥接类/方法名、成功或失败状态，以及 `ret/code/msg` 等字段；不记录调用参数、账号信息或完整响应。WindVane 只是待验证的实际调用路径，不应将缺失事件解释为未发起领取请求。WebView 探针在新建页面时注入，替换 dylib 后须彻底关闭并重开闲鱼，再进入活动页。

日志不包含 URL 查询参数、请求头、Cookie、Token、设备标识或原始响应正文；只提取服务端 `ret`、`code`、`msg` 等错误结果字段（字符串截断为 180 字符）。请仍在分享前核对日志。日志上限 1 MiB，超过后停止记录而非删除已有数据；卸载插件不自动删除日志。

请在安装后重启闲鱼，记录一次点击“领取”的准确时间，再从 Filza 导出上述日志。失败不必然意味着设备检测：需以实际服务端返回码区分登录、资格、当日限额、活动状态或设备风险。
